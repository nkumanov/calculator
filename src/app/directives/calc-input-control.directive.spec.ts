import { CalcInputControlDirective } from './calc-input-control.directive';

describe('CalcInputControlDirective', () => {
  it('should create an instance', () => {
    const directive = new CalcInputControlDirective();
    expect(directive).toBeTruthy();
  });
});
